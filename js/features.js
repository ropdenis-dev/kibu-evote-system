// KIBU eVote - Core Features Only
// No extra features, just the essentials

console.log('KIBU eVote system loaded');

// Any essential helper functions can go here
// All extra features (education center, comparison, map, dark mode, etc.) have been removed

// Keep only what's necessary for your voting system
if (typeof window.API === 'undefined') {
    window.API = window.API || {};
}

console.log('✅ KIBU eVote ready');